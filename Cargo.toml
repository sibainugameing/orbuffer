[package]
name = "orbuffer"
version = "0.1.0"
edition = "2021"
description = "A desktop download manager"
license = "Apache-2.0"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "rustls-tls"] }
url = "2"

[dev-dependencies]
